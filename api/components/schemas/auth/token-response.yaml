type: object
properties:
  accessToken:
    type: string
  refreshToken:
    type: string
  expiresIn:
    type: integer
    description: Expiration of access token in seconds
  tokenType:
    type: string
    enum: [ access, refresh ]