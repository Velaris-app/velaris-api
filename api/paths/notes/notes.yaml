get:
  tags:
    - notes
  summary: Pobranie wszystkich notatek użytkownika
  security:
    - bearerAuth: []
  responses:
    "200":
      description: Lista notatek
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../../components/schemas/note.yaml"
post:
  tags:
    - notes
  summary: Dodanie notatki
  security:
    - bearerAuth: []
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../components/schemas/note.yaml"
  responses:
    "201":
      description: Utworzona notatka
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/note.yaml"