get:
  tags:
    - stats
  summary: Pobranie statystyk portfolio użytkownika
  operationId: getStats
  security:
    - bearerAuth: []
  responses:
    "200":
      description: Statystyki portfolio
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/stat.yaml"