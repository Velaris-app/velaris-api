get:
  tags:
    - stats
  summary: Get top movers (biggest gain/loss)
  operationId: getTopMovers
  security:
    - bearerAuth: [ ]
  responses:
    '200':
      description: List of top movers
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '#/components/schemas/StatsTopMoversItem'