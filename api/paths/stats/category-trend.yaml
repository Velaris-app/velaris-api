get:
  tags:
    - stats
  summary: Get category trend over time
  operationId: getCategoryTrend
  security:
    - bearerAuth: [ ]
  parameters:
    - name: category
      in: query
      required: true
      schema:
        type: string
  responses:
    '200':
      description: List of category trend items
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../components/schemas/stats-category-trend-item.yaml'