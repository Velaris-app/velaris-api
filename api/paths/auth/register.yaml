post:
  tags:
    - auth
  summary: Rejestracja użytkownika
  operationId: register
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../components/schemas/register-request.yaml"
  responses:
    "200":
      description: Zarejestrowany użytkownik + token
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/jwt-response.yaml"